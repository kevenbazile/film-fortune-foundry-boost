
// This file is now just re-exporting from the newly created files
// for backward compatibility
export { ensureBucketExists } from './bucketUtils';
export { uploadFileToStorage } from './uploadUtils';
export { createFilmRecord, addFilmAssets } from './filmDatabase';
export { uploadFilesToStorage } from './fileUploadService';
